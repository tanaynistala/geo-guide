type Country = {
  name: string;
  code3: string;
  code2: string;
  coordinates: number[];
  scale: number;
  continent: string;
  subregion: string;
  capital: string;
  currency: string[];
  tld: string;
  drivesOnLeft: boolean;
};

export default Country;
